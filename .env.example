# L'API passe par le proxy Vite (voir vite.config.ts) → évite les soucis CORS / cookies cross-origin.
# Le proxy /api cible https://localhost:7169 (backend .NET). Garder "/api" en développement.
VITE_API_URL=/api
VITE_MOCK_API=false
VITE_MOCK_DELAY=400
VITE_OVERRIDE_ROLE=true
